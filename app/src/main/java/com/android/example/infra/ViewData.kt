package com.android.example.infra

/**
 * Interface for POJO representations of data needed to render the UI.
 * Produced by transformation through a Transformer. Implementation should be immutable.
 */
interface ViewData